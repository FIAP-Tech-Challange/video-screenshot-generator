apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: dev-

commonLabels:
  environment: development

# Aqui você pode adicionar patches específicos do ambiente de dev
# Por exemplo, para mudar replicas, resources, etc.
