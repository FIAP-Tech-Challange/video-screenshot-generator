apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: video-app
  labels:
    app: frontend
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
  - name: http
    protocol: TCP
    port: 4200
    targetPort: 4200
