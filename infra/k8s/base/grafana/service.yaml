apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: video-app
  labels:
    app: grafana
spec:
  type: LoadBalancer
  selector:
    app: grafana
  ports:
  - name: http
    protocol: TCP
    port: 3005
    targetPort: 3000
