apiVersion: v1
kind: Service
metadata:
  name: api-consumer
  namespace: video-app
spec:
  selector:
    app: api-consumer
  ports:
  - port: 3001
    targetPort: 3001
    name: http
  type: LoadBalancer  # Exposto para Prometheus coletar métricas
