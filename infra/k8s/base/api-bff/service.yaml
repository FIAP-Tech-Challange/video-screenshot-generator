apiVersion: v1
kind: Service
metadata:
  name: api-bff
  namespace: video-app
  labels:
    app: api-bff
spec:
  type: LoadBalancer
  selector:
    app: api-bff
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 3000
